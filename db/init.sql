CREATE TABLE [IF NOT EXISTS] cuenta (
   id_cuenta datatype(length) NOT NULL,
   correo datatype(length) NOT NULL,
   contrasena datatype(length) NOT NULL,
   column2 datatype(length) NOT NULL,
   column3 datatype(length) NOT NULL,
   column2 datatype(length) NOT NULL,
   table_constraints
);
CREATE TABLE [IF NOT EXISTS] diagnostico_medico (
   column1 datatype(length) column_contraint,
   column2 datatype(length) column_contraint,
   column3 datatype(length) column_contraint,
   table_constraints
);
CREATE TABLE [IF NOT EXISTS] paciente (
   column1 datatype(length) column_contraint,
   column2 datatype(length) column_contraint,
   column3 datatype(length) column_contraint,
   table_constraints
);